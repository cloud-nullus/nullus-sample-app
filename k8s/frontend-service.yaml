apiVersion: v1
kind: Service
metadata:
  name: nullus-sample-frontend
  namespace: nullus-sample
spec:
  selector:
    app: nullus-sample-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
