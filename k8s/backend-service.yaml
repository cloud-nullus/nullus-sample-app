apiVersion: v1
kind: Service
metadata:
  name: nullus-sample-backend
  namespace: nullus-sample
spec:
  selector:
    app: nullus-sample-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
